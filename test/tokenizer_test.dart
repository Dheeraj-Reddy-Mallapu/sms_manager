import 'package:bert_tokenizer/bert_tokenizer.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test tokenizer', () async {
    final vocab = 'assets/vocab.txt'; // wait, it might require a map or file
    // Let's just instantiate and print methods.
    print(BertTokenizer);
  });
}
